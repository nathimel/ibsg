defaults: 
  - dynamics: moran_process

# simulation parameters
num_trials: 1

multiprocessing: False # N.B. just for running simulations of dynamics for now, not for e.g. genetic algorithm, analysis
num_processes: null # null defaults to cpu_count